{
  "name": "membership-centre",
  "version": "1.0.0",
  "description": "Customer membership centre extension",
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "generate": "shopify app generate extension"
  }
}